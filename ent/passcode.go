// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gofrs/uuid"
	"github.com/NoahJinnn/passkey_auth_svc/ent/email"
	"github.com/NoahJinnn/passkey_auth_svc/ent/passcode"
	"github.com/NoahJinnn/passkey_auth_svc/ent/user"
)

// Passcode is the model entity for the Passcode schema.
type Passcode struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// TTL holds the value of the "ttl" field.
	TTL int32 `json:"ttl,omitempty"`
	// Code holds the value of the "code" field.
	Code string `json:"code,omitempty"`
	// TryCount holds the value of the "try_count" field.
	TryCount int32 `json:"try_count,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// EmailID holds the value of the "email_id" field.
	EmailID uuid.UUID `json:"email_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PasscodeQuery when eager-loading is set.
	Edges        PasscodeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PasscodeEdges holds the relations/edges for other nodes in the graph.
type PasscodeEdges struct {
	// Email holds the value of the email edge.
	Email *Email `json:"email,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// EmailOrErr returns the Email value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PasscodeEdges) EmailOrErr() (*Email, error) {
	if e.loadedTypes[0] {
		if e.Email == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: email.Label}
		}
		return e.Email, nil
	}
	return nil, &NotLoadedError{edge: "email"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PasscodeEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Passcode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case passcode.FieldTTL, passcode.FieldTryCount:
			values[i] = new(sql.NullInt64)
		case passcode.FieldCode:
			values[i] = new(sql.NullString)
		case passcode.FieldCreatedAt, passcode.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case passcode.FieldID, passcode.FieldUserID, passcode.FieldEmailID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Passcode fields.
func (pa *Passcode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case passcode.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pa.ID = *value
			}
		case passcode.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				pa.UserID = *value
			}
		case passcode.FieldTTL:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ttl", values[i])
			} else if value.Valid {
				pa.TTL = int32(value.Int64)
			}
		case passcode.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				pa.Code = value.String
			}
		case passcode.FieldTryCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field try_count", values[i])
			} else if value.Valid {
				pa.TryCount = int32(value.Int64)
			}
		case passcode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pa.CreatedAt = value.Time
			}
		case passcode.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pa.UpdatedAt = value.Time
			}
		case passcode.FieldEmailID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field email_id", values[i])
			} else if value != nil {
				pa.EmailID = *value
			}
		default:
			pa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Passcode.
// This includes values selected through modifiers, order, etc.
func (pa *Passcode) Value(name string) (ent.Value, error) {
	return pa.selectValues.Get(name)
}

// QueryEmail queries the "email" edge of the Passcode entity.
func (pa *Passcode) QueryEmail() *EmailQuery {
	return NewPasscodeClient(pa.config).QueryEmail(pa)
}

// QueryUser queries the "user" edge of the Passcode entity.
func (pa *Passcode) QueryUser() *UserQuery {
	return NewPasscodeClient(pa.config).QueryUser(pa)
}

// Update returns a builder for updating this Passcode.
// Note that you need to call Passcode.Unwrap() before calling this method if this Passcode
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *Passcode) Update() *PasscodeUpdateOne {
	return NewPasscodeClient(pa.config).UpdateOne(pa)
}

// Unwrap unwraps the Passcode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *Passcode) Unwrap() *Passcode {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: Passcode is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *Passcode) String() string {
	var builder strings.Builder
	builder.WriteString("Passcode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", pa.UserID))
	builder.WriteString(", ")
	builder.WriteString("ttl=")
	builder.WriteString(fmt.Sprintf("%v", pa.TTL))
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(pa.Code)
	builder.WriteString(", ")
	builder.WriteString("try_count=")
	builder.WriteString(fmt.Sprintf("%v", pa.TryCount))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pa.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("email_id=")
	builder.WriteString(fmt.Sprintf("%v", pa.EmailID))
	builder.WriteByte(')')
	return builder.String()
}

// Passcodes is a parsable slice of Passcode.
type Passcodes []*Passcode
