// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hellohq/hqservice/ent/assetinfo"
	"github.com/hellohq/hqservice/ent/predicate"
)

// AssetInfoQuery is the builder for querying AssetInfo entities.
type AssetInfoQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.AssetInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AssetInfoQuery builder.
func (aiq *AssetInfoQuery) Where(ps ...predicate.AssetInfo) *AssetInfoQuery {
	aiq.predicates = append(aiq.predicates, ps...)
	return aiq
}

// Limit the number of records to be returned by this query.
func (aiq *AssetInfoQuery) Limit(limit int) *AssetInfoQuery {
	aiq.ctx.Limit = &limit
	return aiq
}

// Offset to start from.
func (aiq *AssetInfoQuery) Offset(offset int) *AssetInfoQuery {
	aiq.ctx.Offset = &offset
	return aiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aiq *AssetInfoQuery) Unique(unique bool) *AssetInfoQuery {
	aiq.ctx.Unique = &unique
	return aiq
}

// Order specifies how the records should be ordered.
func (aiq *AssetInfoQuery) Order(o ...OrderFunc) *AssetInfoQuery {
	aiq.order = append(aiq.order, o...)
	return aiq
}

// First returns the first AssetInfo entity from the query.
// Returns a *NotFoundError when no AssetInfo was found.
func (aiq *AssetInfoQuery) First(ctx context.Context) (*AssetInfo, error) {
	nodes, err := aiq.Limit(1).All(setContextOp(ctx, aiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{assetinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aiq *AssetInfoQuery) FirstX(ctx context.Context) *AssetInfo {
	node, err := aiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AssetInfo ID from the query.
// Returns a *NotFoundError when no AssetInfo ID was found.
func (aiq *AssetInfoQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = aiq.Limit(1).IDs(setContextOp(ctx, aiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{assetinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aiq *AssetInfoQuery) FirstIDX(ctx context.Context) uint {
	id, err := aiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AssetInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AssetInfo entity is found.
// Returns a *NotFoundError when no AssetInfo entities are found.
func (aiq *AssetInfoQuery) Only(ctx context.Context) (*AssetInfo, error) {
	nodes, err := aiq.Limit(2).All(setContextOp(ctx, aiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{assetinfo.Label}
	default:
		return nil, &NotSingularError{assetinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aiq *AssetInfoQuery) OnlyX(ctx context.Context) *AssetInfo {
	node, err := aiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AssetInfo ID in the query.
// Returns a *NotSingularError when more than one AssetInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (aiq *AssetInfoQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = aiq.Limit(2).IDs(setContextOp(ctx, aiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{assetinfo.Label}
	default:
		err = &NotSingularError{assetinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aiq *AssetInfoQuery) OnlyIDX(ctx context.Context) uint {
	id, err := aiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AssetInfos.
func (aiq *AssetInfoQuery) All(ctx context.Context) ([]*AssetInfo, error) {
	ctx = setContextOp(ctx, aiq.ctx, "All")
	if err := aiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AssetInfo, *AssetInfoQuery]()
	return withInterceptors[[]*AssetInfo](ctx, aiq, qr, aiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aiq *AssetInfoQuery) AllX(ctx context.Context) []*AssetInfo {
	nodes, err := aiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AssetInfo IDs.
func (aiq *AssetInfoQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if aiq.ctx.Unique == nil && aiq.path != nil {
		aiq.Unique(true)
	}
	ctx = setContextOp(ctx, aiq.ctx, "IDs")
	if err = aiq.Select(assetinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aiq *AssetInfoQuery) IDsX(ctx context.Context) []uint {
	ids, err := aiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aiq *AssetInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aiq.ctx, "Count")
	if err := aiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aiq, querierCount[*AssetInfoQuery](), aiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aiq *AssetInfoQuery) CountX(ctx context.Context) int {
	count, err := aiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aiq *AssetInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aiq.ctx, "Exist")
	switch _, err := aiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aiq *AssetInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := aiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AssetInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aiq *AssetInfoQuery) Clone() *AssetInfoQuery {
	if aiq == nil {
		return nil
	}
	return &AssetInfoQuery{
		config:     aiq.config,
		ctx:        aiq.ctx.Clone(),
		order:      append([]OrderFunc{}, aiq.order...),
		inters:     append([]Interceptor{}, aiq.inters...),
		predicates: append([]predicate.AssetInfo{}, aiq.predicates...),
		// clone intermediate query.
		sql:  aiq.sql.Clone(),
		path: aiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AccountInfo struct {} `json:"account_info,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AssetInfo.Query().
//		GroupBy(assetinfo.FieldAccountInfo).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aiq *AssetInfoQuery) GroupBy(field string, fields ...string) *AssetInfoGroupBy {
	aiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AssetInfoGroupBy{build: aiq}
	grbuild.flds = &aiq.ctx.Fields
	grbuild.label = assetinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AccountInfo struct {} `json:"account_info,omitempty"`
//	}
//
//	client.AssetInfo.Query().
//		Select(assetinfo.FieldAccountInfo).
//		Scan(ctx, &v)
func (aiq *AssetInfoQuery) Select(fields ...string) *AssetInfoSelect {
	aiq.ctx.Fields = append(aiq.ctx.Fields, fields...)
	sbuild := &AssetInfoSelect{AssetInfoQuery: aiq}
	sbuild.label = assetinfo.Label
	sbuild.flds, sbuild.scan = &aiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AssetInfoSelect configured with the given aggregations.
func (aiq *AssetInfoQuery) Aggregate(fns ...AggregateFunc) *AssetInfoSelect {
	return aiq.Select().Aggregate(fns...)
}

func (aiq *AssetInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aiq); err != nil {
				return err
			}
		}
	}
	for _, f := range aiq.ctx.Fields {
		if !assetinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aiq.path != nil {
		prev, err := aiq.path(ctx)
		if err != nil {
			return err
		}
		aiq.sql = prev
	}
	return nil
}

func (aiq *AssetInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AssetInfo, error) {
	var (
		nodes = []*AssetInfo{}
		_spec = aiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AssetInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AssetInfo{config: aiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aiq *AssetInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aiq.querySpec()
	_spec.Node.Columns = aiq.ctx.Fields
	if len(aiq.ctx.Fields) > 0 {
		_spec.Unique = aiq.ctx.Unique != nil && *aiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aiq.driver, _spec)
}

func (aiq *AssetInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(assetinfo.Table, assetinfo.Columns, sqlgraph.NewFieldSpec(assetinfo.FieldID, field.TypeUint))
	_spec.From = aiq.sql
	if unique := aiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aiq.path != nil {
		_spec.Unique = true
	}
	if fields := aiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, assetinfo.FieldID)
		for i := range fields {
			if fields[i] != assetinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aiq *AssetInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aiq.driver.Dialect())
	t1 := builder.Table(assetinfo.Table)
	columns := aiq.ctx.Fields
	if len(columns) == 0 {
		columns = assetinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aiq.sql != nil {
		selector = aiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aiq.ctx.Unique != nil && *aiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aiq.predicates {
		p(selector)
	}
	for _, p := range aiq.order {
		p(selector)
	}
	if offset := aiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AssetInfoGroupBy is the group-by builder for AssetInfo entities.
type AssetInfoGroupBy struct {
	selector
	build *AssetInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aigb *AssetInfoGroupBy) Aggregate(fns ...AggregateFunc) *AssetInfoGroupBy {
	aigb.fns = append(aigb.fns, fns...)
	return aigb
}

// Scan applies the selector query and scans the result into the given value.
func (aigb *AssetInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aigb.build.ctx, "GroupBy")
	if err := aigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetInfoQuery, *AssetInfoGroupBy](ctx, aigb.build, aigb, aigb.build.inters, v)
}

func (aigb *AssetInfoGroupBy) sqlScan(ctx context.Context, root *AssetInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aigb.fns))
	for _, fn := range aigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aigb.flds)+len(aigb.fns))
		for _, f := range *aigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AssetInfoSelect is the builder for selecting fields of AssetInfo entities.
type AssetInfoSelect struct {
	*AssetInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ais *AssetInfoSelect) Aggregate(fns ...AggregateFunc) *AssetInfoSelect {
	ais.fns = append(ais.fns, fns...)
	return ais
}

// Scan applies the selector query and scans the result into the given value.
func (ais *AssetInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ais.ctx, "Select")
	if err := ais.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetInfoQuery, *AssetInfoSelect](ctx, ais.AssetInfoQuery, ais, ais.inters, v)
}

func (ais *AssetInfoSelect) sqlScan(ctx context.Context, root *AssetInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ais.fns))
	for _, fn := range ais.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ais.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ais.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
