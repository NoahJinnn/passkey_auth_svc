// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gofrs/uuid"
	"github.com/hellohq/hqservice/internal/db/sqlite/ent/manualitem"
	"github.com/hellohq/hqservice/internal/db/sqlite/ent/predicate"
)

// ManualItemQuery is the builder for querying ManualItem entities.
type ManualItemQuery struct {
	config
	ctx        *QueryContext
	order      []manualitem.OrderOption
	inters     []Interceptor
	predicates []predicate.ManualItem
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ManualItemQuery builder.
func (miq *ManualItemQuery) Where(ps ...predicate.ManualItem) *ManualItemQuery {
	miq.predicates = append(miq.predicates, ps...)
	return miq
}

// Limit the number of records to be returned by this query.
func (miq *ManualItemQuery) Limit(limit int) *ManualItemQuery {
	miq.ctx.Limit = &limit
	return miq
}

// Offset to start from.
func (miq *ManualItemQuery) Offset(offset int) *ManualItemQuery {
	miq.ctx.Offset = &offset
	return miq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (miq *ManualItemQuery) Unique(unique bool) *ManualItemQuery {
	miq.ctx.Unique = &unique
	return miq
}

// Order specifies how the records should be ordered.
func (miq *ManualItemQuery) Order(o ...manualitem.OrderOption) *ManualItemQuery {
	miq.order = append(miq.order, o...)
	return miq
}

// First returns the first ManualItem entity from the query.
// Returns a *NotFoundError when no ManualItem was found.
func (miq *ManualItemQuery) First(ctx context.Context) (*ManualItem, error) {
	nodes, err := miq.Limit(1).All(setContextOp(ctx, miq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{manualitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (miq *ManualItemQuery) FirstX(ctx context.Context) *ManualItem {
	node, err := miq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ManualItem ID from the query.
// Returns a *NotFoundError when no ManualItem ID was found.
func (miq *ManualItemQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = miq.Limit(1).IDs(setContextOp(ctx, miq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{manualitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (miq *ManualItemQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := miq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ManualItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ManualItem entity is found.
// Returns a *NotFoundError when no ManualItem entities are found.
func (miq *ManualItemQuery) Only(ctx context.Context) (*ManualItem, error) {
	nodes, err := miq.Limit(2).All(setContextOp(ctx, miq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{manualitem.Label}
	default:
		return nil, &NotSingularError{manualitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (miq *ManualItemQuery) OnlyX(ctx context.Context) *ManualItem {
	node, err := miq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ManualItem ID in the query.
// Returns a *NotSingularError when more than one ManualItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (miq *ManualItemQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = miq.Limit(2).IDs(setContextOp(ctx, miq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{manualitem.Label}
	default:
		err = &NotSingularError{manualitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (miq *ManualItemQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := miq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ManualItems.
func (miq *ManualItemQuery) All(ctx context.Context) ([]*ManualItem, error) {
	ctx = setContextOp(ctx, miq.ctx, "All")
	if err := miq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ManualItem, *ManualItemQuery]()
	return withInterceptors[[]*ManualItem](ctx, miq, qr, miq.inters)
}

// AllX is like All, but panics if an error occurs.
func (miq *ManualItemQuery) AllX(ctx context.Context) []*ManualItem {
	nodes, err := miq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ManualItem IDs.
func (miq *ManualItemQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if miq.ctx.Unique == nil && miq.path != nil {
		miq.Unique(true)
	}
	ctx = setContextOp(ctx, miq.ctx, "IDs")
	if err = miq.Select(manualitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (miq *ManualItemQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := miq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (miq *ManualItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, miq.ctx, "Count")
	if err := miq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, miq, querierCount[*ManualItemQuery](), miq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (miq *ManualItemQuery) CountX(ctx context.Context) int {
	count, err := miq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (miq *ManualItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, miq.ctx, "Exist")
	switch _, err := miq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (miq *ManualItemQuery) ExistX(ctx context.Context) bool {
	exist, err := miq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ManualItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (miq *ManualItemQuery) Clone() *ManualItemQuery {
	if miq == nil {
		return nil
	}
	return &ManualItemQuery{
		config:     miq.config,
		ctx:        miq.ctx.Clone(),
		order:      append([]manualitem.OrderOption{}, miq.order...),
		inters:     append([]Interceptor{}, miq.inters...),
		predicates: append([]predicate.ManualItem{}, miq.predicates...),
		// clone intermediate query.
		sql:  miq.sql.Clone(),
		path: miq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProviderName string `json:"provider_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ManualItem.Query().
//		GroupBy(manualitem.FieldProviderName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (miq *ManualItemQuery) GroupBy(field string, fields ...string) *ManualItemGroupBy {
	miq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ManualItemGroupBy{build: miq}
	grbuild.flds = &miq.ctx.Fields
	grbuild.label = manualitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProviderName string `json:"provider_name,omitempty"`
//	}
//
//	client.ManualItem.Query().
//		Select(manualitem.FieldProviderName).
//		Scan(ctx, &v)
func (miq *ManualItemQuery) Select(fields ...string) *ManualItemSelect {
	miq.ctx.Fields = append(miq.ctx.Fields, fields...)
	sbuild := &ManualItemSelect{ManualItemQuery: miq}
	sbuild.label = manualitem.Label
	sbuild.flds, sbuild.scan = &miq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ManualItemSelect configured with the given aggregations.
func (miq *ManualItemQuery) Aggregate(fns ...AggregateFunc) *ManualItemSelect {
	return miq.Select().Aggregate(fns...)
}

func (miq *ManualItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range miq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, miq); err != nil {
				return err
			}
		}
	}
	for _, f := range miq.ctx.Fields {
		if !manualitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if miq.path != nil {
		prev, err := miq.path(ctx)
		if err != nil {
			return err
		}
		miq.sql = prev
	}
	return nil
}

func (miq *ManualItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ManualItem, error) {
	var (
		nodes = []*ManualItem{}
		_spec = miq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ManualItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ManualItem{config: miq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, miq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (miq *ManualItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := miq.querySpec()
	_spec.Node.Columns = miq.ctx.Fields
	if len(miq.ctx.Fields) > 0 {
		_spec.Unique = miq.ctx.Unique != nil && *miq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, miq.driver, _spec)
}

func (miq *ManualItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(manualitem.Table, manualitem.Columns, sqlgraph.NewFieldSpec(manualitem.FieldID, field.TypeUUID))
	_spec.From = miq.sql
	if unique := miq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if miq.path != nil {
		_spec.Unique = true
	}
	if fields := miq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, manualitem.FieldID)
		for i := range fields {
			if fields[i] != manualitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := miq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := miq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := miq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := miq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (miq *ManualItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(miq.driver.Dialect())
	t1 := builder.Table(manualitem.Table)
	columns := miq.ctx.Fields
	if len(columns) == 0 {
		columns = manualitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if miq.sql != nil {
		selector = miq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if miq.ctx.Unique != nil && *miq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range miq.predicates {
		p(selector)
	}
	for _, p := range miq.order {
		p(selector)
	}
	if offset := miq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := miq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ManualItemGroupBy is the group-by builder for ManualItem entities.
type ManualItemGroupBy struct {
	selector
	build *ManualItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (migb *ManualItemGroupBy) Aggregate(fns ...AggregateFunc) *ManualItemGroupBy {
	migb.fns = append(migb.fns, fns...)
	return migb
}

// Scan applies the selector query and scans the result into the given value.
func (migb *ManualItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, migb.build.ctx, "GroupBy")
	if err := migb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualItemQuery, *ManualItemGroupBy](ctx, migb.build, migb, migb.build.inters, v)
}

func (migb *ManualItemGroupBy) sqlScan(ctx context.Context, root *ManualItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(migb.fns))
	for _, fn := range migb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*migb.flds)+len(migb.fns))
		for _, f := range *migb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*migb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := migb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ManualItemSelect is the builder for selecting fields of ManualItem entities.
type ManualItemSelect struct {
	*ManualItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mis *ManualItemSelect) Aggregate(fns ...AggregateFunc) *ManualItemSelect {
	mis.fns = append(mis.fns, fns...)
	return mis
}

// Scan applies the selector query and scans the result into the given value.
func (mis *ManualItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mis.ctx, "Select")
	if err := mis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualItemQuery, *ManualItemSelect](ctx, mis.ManualItemQuery, mis, mis.inters, v)
}

func (mis *ManualItemSelect) sqlScan(ctx context.Context, root *ManualItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mis.fns))
	for _, fn := range mis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
