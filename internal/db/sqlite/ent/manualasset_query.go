// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gofrs/uuid"
	"github.com/hellohq/hqservice/internal/db/sqlite/ent/manualasset"
	"github.com/hellohq/hqservice/internal/db/sqlite/ent/predicate"
)

// ManualAssetQuery is the builder for querying ManualAsset entities.
type ManualAssetQuery struct {
	config
	ctx        *QueryContext
	order      []manualasset.OrderOption
	inters     []Interceptor
	predicates []predicate.ManualAsset
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ManualAssetQuery builder.
func (maq *ManualAssetQuery) Where(ps ...predicate.ManualAsset) *ManualAssetQuery {
	maq.predicates = append(maq.predicates, ps...)
	return maq
}

// Limit the number of records to be returned by this query.
func (maq *ManualAssetQuery) Limit(limit int) *ManualAssetQuery {
	maq.ctx.Limit = &limit
	return maq
}

// Offset to start from.
func (maq *ManualAssetQuery) Offset(offset int) *ManualAssetQuery {
	maq.ctx.Offset = &offset
	return maq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (maq *ManualAssetQuery) Unique(unique bool) *ManualAssetQuery {
	maq.ctx.Unique = &unique
	return maq
}

// Order specifies how the records should be ordered.
func (maq *ManualAssetQuery) Order(o ...manualasset.OrderOption) *ManualAssetQuery {
	maq.order = append(maq.order, o...)
	return maq
}

// First returns the first ManualAsset entity from the query.
// Returns a *NotFoundError when no ManualAsset was found.
func (maq *ManualAssetQuery) First(ctx context.Context) (*ManualAsset, error) {
	nodes, err := maq.Limit(1).All(setContextOp(ctx, maq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{manualasset.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (maq *ManualAssetQuery) FirstX(ctx context.Context) *ManualAsset {
	node, err := maq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ManualAsset ID from the query.
// Returns a *NotFoundError when no ManualAsset ID was found.
func (maq *ManualAssetQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = maq.Limit(1).IDs(setContextOp(ctx, maq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{manualasset.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (maq *ManualAssetQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := maq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ManualAsset entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ManualAsset entity is found.
// Returns a *NotFoundError when no ManualAsset entities are found.
func (maq *ManualAssetQuery) Only(ctx context.Context) (*ManualAsset, error) {
	nodes, err := maq.Limit(2).All(setContextOp(ctx, maq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{manualasset.Label}
	default:
		return nil, &NotSingularError{manualasset.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (maq *ManualAssetQuery) OnlyX(ctx context.Context) *ManualAsset {
	node, err := maq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ManualAsset ID in the query.
// Returns a *NotSingularError when more than one ManualAsset ID is found.
// Returns a *NotFoundError when no entities are found.
func (maq *ManualAssetQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = maq.Limit(2).IDs(setContextOp(ctx, maq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{manualasset.Label}
	default:
		err = &NotSingularError{manualasset.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (maq *ManualAssetQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := maq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ManualAssets.
func (maq *ManualAssetQuery) All(ctx context.Context) ([]*ManualAsset, error) {
	ctx = setContextOp(ctx, maq.ctx, "All")
	if err := maq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ManualAsset, *ManualAssetQuery]()
	return withInterceptors[[]*ManualAsset](ctx, maq, qr, maq.inters)
}

// AllX is like All, but panics if an error occurs.
func (maq *ManualAssetQuery) AllX(ctx context.Context) []*ManualAsset {
	nodes, err := maq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ManualAsset IDs.
func (maq *ManualAssetQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if maq.ctx.Unique == nil && maq.path != nil {
		maq.Unique(true)
	}
	ctx = setContextOp(ctx, maq.ctx, "IDs")
	if err = maq.Select(manualasset.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (maq *ManualAssetQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := maq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (maq *ManualAssetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, maq.ctx, "Count")
	if err := maq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, maq, querierCount[*ManualAssetQuery](), maq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (maq *ManualAssetQuery) CountX(ctx context.Context) int {
	count, err := maq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (maq *ManualAssetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, maq.ctx, "Exist")
	switch _, err := maq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (maq *ManualAssetQuery) ExistX(ctx context.Context) bool {
	exist, err := maq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ManualAssetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (maq *ManualAssetQuery) Clone() *ManualAssetQuery {
	if maq == nil {
		return nil
	}
	return &ManualAssetQuery{
		config:     maq.config,
		ctx:        maq.ctx.Clone(),
		order:      append([]manualasset.OrderOption{}, maq.order...),
		inters:     append([]Interceptor{}, maq.inters...),
		predicates: append([]predicate.ManualAsset{}, maq.predicates...),
		// clone intermediate query.
		sql:  maq.sql.Clone(),
		path: maq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProviderName string `json:"provider_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ManualAsset.Query().
//		GroupBy(manualasset.FieldProviderName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (maq *ManualAssetQuery) GroupBy(field string, fields ...string) *ManualAssetGroupBy {
	maq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ManualAssetGroupBy{build: maq}
	grbuild.flds = &maq.ctx.Fields
	grbuild.label = manualasset.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProviderName string `json:"provider_name,omitempty"`
//	}
//
//	client.ManualAsset.Query().
//		Select(manualasset.FieldProviderName).
//		Scan(ctx, &v)
func (maq *ManualAssetQuery) Select(fields ...string) *ManualAssetSelect {
	maq.ctx.Fields = append(maq.ctx.Fields, fields...)
	sbuild := &ManualAssetSelect{ManualAssetQuery: maq}
	sbuild.label = manualasset.Label
	sbuild.flds, sbuild.scan = &maq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ManualAssetSelect configured with the given aggregations.
func (maq *ManualAssetQuery) Aggregate(fns ...AggregateFunc) *ManualAssetSelect {
	return maq.Select().Aggregate(fns...)
}

func (maq *ManualAssetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range maq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, maq); err != nil {
				return err
			}
		}
	}
	for _, f := range maq.ctx.Fields {
		if !manualasset.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if maq.path != nil {
		prev, err := maq.path(ctx)
		if err != nil {
			return err
		}
		maq.sql = prev
	}
	return nil
}

func (maq *ManualAssetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ManualAsset, error) {
	var (
		nodes = []*ManualAsset{}
		_spec = maq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ManualAsset).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ManualAsset{config: maq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, maq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (maq *ManualAssetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := maq.querySpec()
	_spec.Node.Columns = maq.ctx.Fields
	if len(maq.ctx.Fields) > 0 {
		_spec.Unique = maq.ctx.Unique != nil && *maq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, maq.driver, _spec)
}

func (maq *ManualAssetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(manualasset.Table, manualasset.Columns, sqlgraph.NewFieldSpec(manualasset.FieldID, field.TypeUUID))
	_spec.From = maq.sql
	if unique := maq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if maq.path != nil {
		_spec.Unique = true
	}
	if fields := maq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, manualasset.FieldID)
		for i := range fields {
			if fields[i] != manualasset.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := maq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := maq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := maq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := maq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (maq *ManualAssetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(maq.driver.Dialect())
	t1 := builder.Table(manualasset.Table)
	columns := maq.ctx.Fields
	if len(columns) == 0 {
		columns = manualasset.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if maq.sql != nil {
		selector = maq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if maq.ctx.Unique != nil && *maq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range maq.predicates {
		p(selector)
	}
	for _, p := range maq.order {
		p(selector)
	}
	if offset := maq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := maq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ManualAssetGroupBy is the group-by builder for ManualAsset entities.
type ManualAssetGroupBy struct {
	selector
	build *ManualAssetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (magb *ManualAssetGroupBy) Aggregate(fns ...AggregateFunc) *ManualAssetGroupBy {
	magb.fns = append(magb.fns, fns...)
	return magb
}

// Scan applies the selector query and scans the result into the given value.
func (magb *ManualAssetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, magb.build.ctx, "GroupBy")
	if err := magb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualAssetQuery, *ManualAssetGroupBy](ctx, magb.build, magb, magb.build.inters, v)
}

func (magb *ManualAssetGroupBy) sqlScan(ctx context.Context, root *ManualAssetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(magb.fns))
	for _, fn := range magb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*magb.flds)+len(magb.fns))
		for _, f := range *magb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*magb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := magb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ManualAssetSelect is the builder for selecting fields of ManualAsset entities.
type ManualAssetSelect struct {
	*ManualAssetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mas *ManualAssetSelect) Aggregate(fns ...AggregateFunc) *ManualAssetSelect {
	mas.fns = append(mas.fns, fns...)
	return mas
}

// Scan applies the selector query and scans the result into the given value.
func (mas *ManualAssetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mas.ctx, "Select")
	if err := mas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualAssetQuery, *ManualAssetSelect](ctx, mas.ManualAssetQuery, mas, mas.inters, v)
}

func (mas *ManualAssetSelect) sqlScan(ctx context.Context, root *ManualAssetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mas.fns))
	for _, fn := range mas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
