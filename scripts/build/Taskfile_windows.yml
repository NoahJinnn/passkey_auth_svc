version: '3'

tasks:
  binary:
    desc: "Build binary"
    cmds:
      - ./build/sh/build.sh {{OS}} {{ARCH}} {{.CLI_ARGS}}

  container:
    desc: "Build binary and container"
    cmds:
      - ./build/sh/build_container.sh