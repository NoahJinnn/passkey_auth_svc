version: '3'

tasks:
  hadolint:
    desc: "Install hadolint on windows"
    cmds:
      - scoop install hadolint
  
  postgresql:
    desc: "Install postgresql on windows"
    cmds:
      - scoop install postgresql@15

  gotools:
    desc: "Install golang tools"
    cmds:
      - ./install/sh/gotools.sh