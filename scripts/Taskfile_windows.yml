version: '3'

includes:
  install: ./install/Taskfile_{{OS}}.yml
  build: ./build/Taskfile_{{OS}}.yml

tasks:
  run:
    desc: "Go run app with dev env"
    cmds:
      - ./sh/run_macos "{{.CLI_ARGS}}" # TODO: Implement run script for Windows
      
  test:
    desc: "Run test"
    cmds:
        - ./sh/test
  stat:
    desc: "Get code statistics"
    cmds:
      - ./sh/stat_linux